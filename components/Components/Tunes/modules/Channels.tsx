import { FunctionComponent } from "react";
import { ChannelsProps } from "../types/video.types";

const Channels: FunctionComponent<ChannelsProps> = (): JSX.Element => {
  return <div></div>;
};

export default Channels;
